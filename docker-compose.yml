services:
  jobsentinel-linkedin:
    build: .
    command: ["/app/scripts/start-xvfb.sh", "python", "-m", "src.core.controller", "--platforms", "linkedin"]
    env_file:
      - .env
    environment:
      - JOBSENTINEL_PLATFORMS=linkedin
    volumes:
      - ./sessions:/app/sessions
      - ./data:/app/data
      - ./configs:/app/configs
  jobsentinel-naukri:
    build: .
    command: ["/app/scripts/start-xvfb.sh", "python", "-m", "src.core.controller", "--platforms", "naukri"]
    env_file:
      - .env
    environment:
      - JOBSENTINEL_PLATFORMS=naukri
    volumes:
      - ./sessions:/app/sessions
      - ./data:/app/data
      - ./configs:/app/configs
  jobsentinel-indeed:
    build: .
    command: ["/app/scripts/start-xvfb.sh", "python", "-m", "src.core.controller", "--platforms", "indeed"]
    env_file:
      - .env
    environment:
      - JOBSENTINEL_PLATFORMS=indeed
    volumes:
      - ./sessions:/app/sessions
      - ./data:/app/data
      - ./configs:/app/configs
  dashboard:
    build: .
    command: ["/app/scripts/start-dashboard-gui.sh", "python", "-m", "dashboard.app"]
    env_file:
      - .env
    environment:
      - JOBSENTINEL_VNC_URL=http://localhost:6080/vnc.html?autoconnect=1&resize=scale
    volumes:
      - ./sessions:/app/sessions
      - ./data:/app/data
      - ./configs:/app/configs
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "0.0.0.0:5000:5000"
      - "0.0.0.0:6080:6080"
    network_mode: bridge
